apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard-complete-direct
  namespace: kubernetes-dashboard
spec:
  entryPoints:
    - websecure
  routes:
    # Auth routes - highest priority
    - match: Host(`dashboard.pindaroli.org`) && PathPrefix(`/api/v1/csrftoken`)
      kind: Rule
      priority: 100
      middlewares:
        - name: dashboard-cors
      services:
        - name: kubernetes-dashboard-auth
          port: 8000
    - match: Host(`dashboard.pindaroli.org`) && PathPrefix(`/api/v1/login`)
      kind: Rule
      priority: 100
      middlewares:
        - name: dashboard-cors
      services:
        - name: kubernetes-dashboard-auth
          port: 8000
    - match: Host(`dashboard.pindaroli.org`) && PathPrefix(`/api/v1/me`)
      kind: Rule
      priority: 100
      middlewares:
        - name: dashboard-cors
      services:
        - name: kubernetes-dashboard-auth
          port: 8000
    # API routes - medium priority
    - match: Host(`dashboard.pindaroli.org`) && PathPrefix(`/api`)
      kind: Rule
      priority: 50
      middlewares:
        - name: dashboard-cors
      services:
        - name: kubernetes-dashboard-api
          port: 8000
    # Web routes - lowest priority (catch-all)
    - match: Host(`dashboard.pindaroli.org`)
      kind: Rule
      priority: 10
      middlewares:
        - name: dashboard-cors
      services:
        - name: kubernetes-dashboard-web
          port: 8000
  tls:
    secretName: pindaroli-wildcard-tls