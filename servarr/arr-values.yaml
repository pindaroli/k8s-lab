jellyfin:
  env:
    TZ: Europe/Rome
  service:
    type: ClusterIP
    externalTrafficPolicy: 
  persistence:
    media:
      existingClaim: servarr-jellyfin-media
    config:
      storageClass: nfs-subdir

sonarr:
  env:
    TZ: Europe/Rome
  persistence:
    existingClaim: servarr-jellyfin-media

qbittorrent:
  env:
    TZ: Europe/Rome
  persistence:
    existingClaim: servarr-jellyfin-media

prowlarr:
  env:
    TZ: Europe/Rome
  persistence:
    storageClass: nfs-subdir

jellyseerr:
  env:
    TZ: Europe/Rome
  persistence:
    storageClass: nfs-subdir

bazarr:
  env:
    TZ: Europe/Rome
  persistence:
    storageClass: nfs-subdir

radarr:
  env:
    TZ: Europe/Rome
  persistence:
    existingClaim: servarr-jellyfin-media

readarr:
  env:
    TZ: Europe/Rome
  persistence:
    existingClaim: servarr-jellyfin-media


lidarr:
  env:
    TZ: Europe/Rome
  persistence:
    existingClaim: servarr-jellyfin-media


