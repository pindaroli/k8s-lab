apiVersion: batch/v1
kind: Job
metadata:
  name: lidarr-migrate
  namespace: arr
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pgloader
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          image: ghcr.io/dimitri/pgloader:3.6.9
          command:
            - pgloader
            - --with
            - "data only"
            - --with
            - "truncate"
            - --with
            - "quote identifiers"
            - /config/lidarr.db
            - postgresql://lidarr:lidarr@postgres-main-rw.cnpg-system.svc.cluster.local:5432/lidarr
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: servarr-lidarr
