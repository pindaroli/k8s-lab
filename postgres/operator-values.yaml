# CloudNativePG Operator Values
# Default values are usually sufficient.
# We explicitly define the controller configuration here for future adjustments.

config:
  # configMapName: "cnpg-controller-manager-config"
  # secretName: "cnpg-controller-manager-secret"
  pass: ""

replicaCount: 1

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 50m
    memory: 256Mi
