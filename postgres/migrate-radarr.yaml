apiVersion: batch/v1
kind: Job
metadata:
  name: radarr-migrate
  namespace: arr
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pgloader
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          image: ghcr.io/dimitri/pgloader:latest
          command:
            - pgloader
            - --with
            - "data only"
            - --with
            - "truncate"
            - --with
            - "quote identifiers"
            - /config/radarr.db
            - postgresql://radarr:radarr@10.108.78.94:5432/radarr
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: servarr-radarr
