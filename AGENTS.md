# k8s-lab — istruzioni agente (Cursor)

Segui `GEMINI.md` in root come unico manuale operativo (golden rules, wiki-first, mandati MCP/Helm).

Non duplicare qui le regole di `GEMINI.md`. Se una regola deve cambiare, si cambia **solo** `GEMINI.md`.
