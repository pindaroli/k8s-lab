apiVersion: apps/v1
kind: Deployment
metadata:
  name: wetty-k8s-runner-1
  namespace: wetty
  labels:
    app: wetty-k8s-runner-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wetty-k8s-runner-1
  template:
    metadata:
      labels:
        app: wetty-k8s-runner-1
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      hostAliases:
      - ip: "192.168.1.11"
        hostnames:
        - "k8s-control"
      - ip: "192.168.1.249" 
        hostnames:
        - "k8s-runner-1"
      containers:
      - name: wetty
        image: wettyoss/wetty:latest
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        command:
        - sh
        - -c
        - |
          cp /root/.ssh/keys/ssh-privatekey /tmp/wetty_key
          chmod 600 /tmp/wetty_key
          exec node . --ssh-host k8s-runner-1 --ssh-user root --ssh-auth publickey --ssh-key /tmp/wetty_key --port 3001 --title "Terminal - K8s Runner" --base /
        volumeMounts:
        - name: ssh-key-rsa
          mountPath: /root/.ssh/keys
          readOnly: true
        - name: ssh-config
          mountPath: /root/.ssh/ssh_config
          readOnly: true
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsUser: 0
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: ssh-key-rsa
        secret:
          secretName: wetty-ssh-key
          defaultMode: 0400
      - name: ssh-config
        configMap:
          name: wetty-config
---
apiVersion: v1
kind: Service
metadata:
  name: wetty-k8s-runner-1-service
  namespace: wetty
  labels:
    app: wetty-k8s-runner-1
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: wetty-k8s-runner-1
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wetty-k8s-runner-1-ingressroute
  namespace: wetty
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`k8s-runner-1.pindaroli.org`)
      kind: Rule
      services:
        - name: wetty-k8s-runner-1-service
          port: 80
      middlewares:
        - name: oauth2-auth
        - name: wetty-headers
  tls:
    secretName: pindaroli-wildcard-tls