---
# Playbook to test connectivity to all hosts
- name: Ping all hosts in inventory
  hosts: all
  gather_facts: no
  tasks:
    - name: Ping test
      ping:
      register: ping_result

    - name: Display ping result
      debug:
        msg: "{{ inventory_hostname }} is reachable"
      when: ping_result is success
